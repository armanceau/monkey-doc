# @monkey-doc/core

Core utilities for [monkey-doc](https://www.npmjs.com/package/monkey-doc) — MDX parsing and filesystem scanning.

This is an internal package consumed by the `monkey-doc` CLI. Install the CLI instead:

```bash
npm install -g monkey-doc@latest
```
