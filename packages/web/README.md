# @monkey-doc/web

React + Vite UI for [monkey-doc](https://www.npmjs.com/package/monkey-doc).

This is an internal package consumed by the `monkey-doc` CLI. Install the CLI instead:

```bash
npm install -g monkey-doc@latest
```
